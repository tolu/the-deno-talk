// lint and type checking

// fix by formatting
const fmt = () => {

  // remove empty lines above and below

}

// flagged by deno lint
let lintError: number = 1;

// flagged by deno run/bundle
let typeError: number = '1';
